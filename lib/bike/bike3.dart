import 'package:flutter/material.dart';

class Bike3 extends StatelessWidget {
  const Bike3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bike3'),
    );
  }
}
