import 'package:flutter/material.dart';

class Bike1 extends StatelessWidget {
  const Bike1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bike1'),
    );
  }
}
