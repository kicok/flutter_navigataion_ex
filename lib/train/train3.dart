import 'package:flutter/material.dart';

class Train3 extends StatelessWidget {
  const Train3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Train3'),
    );
  }
}
