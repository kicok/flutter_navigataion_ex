import 'package:flutter/material.dart';

class Train2 extends StatelessWidget {
  const Train2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Train2'),
    );
  }
}
